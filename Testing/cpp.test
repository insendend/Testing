Какой из ниже перечисленных операторов, не является циклом в С++?
do while
while
for
repeat until (верно)

Название С++ предложил
Бьерн Страуструп (верно)
Кэн Томпсон
Рик Масситти
Дональд Кнут

Какой из следующих операторов - оператор сравнения двух переменных?
=
:=
== (верно)
equal

Какой из перечисленных типов данных не является типом данных в С++?
double
int
real (верно)
float

Какие служебные символы используются для обозначения начала и конца блока кода?
< >
begin end
( )
{ } (верно)

Какая из следующих записей - правильный комментарий в С++?
{комментарий}
/* комментарий */ (верно)
** Комментарий **
*/ Комментарии */

Какое значение, по умолчанию, возвращает программа операционной системе в случае успешного завершения?
-1
0 (верно)
1
Программа не возвращает значение.

Какое из следующих значений эквивалентно зарезервированному слову true?
-1
0.1
1
Все варианты ответов (верно)
66

Что означает константа ios_base::ate, передаваемая в конструктор, в качестве аргумента?
Открыть файл, не создавая его.
Открыть файл, предварительно создав его.
Открыть файл только для чтения.
При открытии переместить указатель в конец файла. (верно)

Что значит ключевое слово inline?
все вызовы встроенных функции заменяются кодом этой функции (верно)
Сообщает компилятору использовать функцию только в пределах одного файла с исходным кодом
нет правильного ответа
